import React from 'react';
import MiniDrawer from '../Drawer/Drawer';

function Dashboard() {
	return (
		<MiniDrawer>
			If you are seeing this means you are logged in and you are in dashboard
		</MiniDrawer>
	);
}

export default Dashboard;
